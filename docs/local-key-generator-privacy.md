# 网页内置密钥生成器说明

server-kit 管理网站会在当前浏览器中生成 AWG 客户端密钥和配置，不需要安装浏览器扩展或访问第三方服务。

私钥仅用于生成客户端配置；提交给服务器的登记信息只包含节点名称、虚拟 IP、公钥和预共享密钥。临时配置最多保存在当前标签页的会话存储 5 分钟，关闭或到期后清除。

必须在登记前下载或扫码导入至少一个客户端配置。VPS 不提供客户端持钥节点的私钥找回能力。
